from setuptools import setup, find_packages

setup(
    name="compute_sdk",
    version="1.0.5",
    description="Mock compute_sdk for local Darwin development",
    packages=find_packages(),
    python_requires='>=3.8',
)


