<!-- Example only; delete or replace when adding real capabilities. -->

# _example Specification

## Purpose

Minimal example spec to show the expected structure. Replace `_example` with a real capability name (e.g. `auth-session`, `catalog-api`).

## Requirements

### Requirement: Example behavior

The system SHALL satisfy one example requirement for illustration.

## Scenarios

#### Scenario: Example scenario

- GIVEN the initial context exists
- WHEN the trigger action occurs
- THEN the expected outcome is observed

## Out of scope

- This is a template; no real behavior is specified.
