# Example change proposal

<!-- Example only; replace with a real change-id and content when using the workflow. -->

## Summary

This is a minimal example change. It illustrates the expected layout under `openspec/changes/<change-id>/`. For a real change, describe what you're changing and why, and reference the affected capability (e.g. `openspec/specs/_example/spec.md`).

## Change ID

`_example`

## Affected capability

`_example` (see `openspec/specs/_example/spec.md` for current spec).
